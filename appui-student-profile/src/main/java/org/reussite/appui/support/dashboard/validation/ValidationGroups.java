package org.reussite.appui.support.dashboard.validation;

import javax.validation.groups.Default;

public interface ValidationGroups {
    interface Patch extends Default { 
    }
    interface Post extends Default { 
    }
}